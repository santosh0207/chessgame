export const miniMax = ()=>{


}
export const min =(game, depth)=>{
    if(depth === 0 /*|| game over */){
        return //will return here something
    }

    let lowestValue = Number.MAX_VALUE;
    //for(let i=0;i<)
}

export const max =(game,depth)=>{
    if(depth === 0 /*|| game over */){
        return //will return here something
    }
}